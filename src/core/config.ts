export const APP_CONFIG = {
  appName: 'Github repositories search',
  shortAppName: 'GRSearch',
};

export const DEFAULT_LIMIT = 20;

export const MINUTE_IN_MS = 60 * 1000;
export const HOUR_IN_MS = 60 * MINUTE_IN_MS;
